<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
 <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
           <a class="bg-red-500 rounded-md px-5 py-2 text-sm mb-5 inline-block" href="/logout">logout</a>
        </div>
        <h5 class="mb-5">Edit Your Post.</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's ON you mind ?.." class="block p-3 w-1/3 outline-none resize-none bg-transparent border-2 border-zinc-700 rounded-md" name="content"><%=  post.content%></textarea>
            <input class="mt-5 px-5 py-2 bg-yellow-500 text-black font-bold block rounded-md" type="submit" value="Update post">
        </form>
        </div>
    </div>
</body>
</html>